{
  "name": "02 — Weekly Digest Generator",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "digest",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "f7e993a7-29ea-4eb1-8a93-a4c4e4bc6119",
      "name": "Digest Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        2368,
        240
      ],
      "webhookId": "digest"
    },
    {
      "parameters": {},
      "id": "04a9487a-def4-408c-a747-c2e623ec9af6",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        2368,
        432
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 9 * * 1"
            }
          ]
        }
      },
      "id": "fa9ac4a7-67e1-4275-b7c4-9815a3a90bd5",
      "name": "Weekly (Mon 9AM)",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        2368,
        640
      ]
    },
    {
      "parameters": {
        "jsCode": "// Normalize webhook body (topic + range) for digest query.\n// Output: topicSlug (string), range (1d|3d|1w|2w), rangeStart (ISO timestamp for Postgres).\nconst item = $input.first();\nconst raw = item?.json || {};\nconst body = raw.body || raw;\nconst topicRaw = body.topicSlug ?? body.topic ?? raw.topicSlug ?? raw.topic ?? body.query?.topic ?? raw.query?.topic;\nconst topicSlug = topicRaw != null && String(topicRaw).trim() ? String(topicRaw).trim() : '';\nconst range = (body.range || body.query?.range || raw.range || '1w').toString().toLowerCase().trim();\nlet days = 7;\nif (range === '2w') days = 14;\nelse if (range === '1d') days = 1;\nelse if (range === '3d') days = 3;\nconst endDate = new Date();\nconst rangeStart = new Date();\nrangeStart.setDate(rangeStart.getDate() - days);\nconst rangeStartISO = rangeStart.toISOString();\nreturn [{ json: { topicSlug, range, rangeStart: rangeStartISO } }];"
      },
      "id": "2f76b8a9-c6ca-40d9-9e44-49bd27b2640f",
      "name": "Normalize Digest Params",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2528,
        544
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT a.id, a.title, a.url, a.source, a.published_at, a.category, a.summary\nFROM articles a\nWHERE a.published_at >= $2::timestamptz\nAND ($1 = '' OR a.id IN (SELECT at.article_id FROM article_topics at INNER JOIN topics t ON t.id = at.topic_id WHERE t.slug = $1))\nORDER BY a.published_at DESC",
        "options": {
          "queryReplacement": "={{ $json.topicSlug }},{{ $json.rangeStart }}"
        }
      },
      "id": "053f7c70-a90a-458b-b4ad-5b98f96d8f59",
      "name": "Fetch Last 7 Days Articles",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        2768,
        544
      ],
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "6hsqSZA38qSubjaf",
          "name": "AI Digest App"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Replicate weeklyDigest.js generateMarkdown() logic; title and date range from Normalize Digest Params.\nconst params = $('Normalize Digest Params').first().json;\nconst topicSlug = params.topicSlug || '';\nconst range = params.range || '1w';\nconst rangeStart = new Date(params.rangeStart);\nconst endDate = new Date();\nconst rangeLabels = { '1d': 'Last 1 day', '3d': 'Last 3 days', '1w': 'Last 1 week', '2w': 'Last 2 weeks' };\nconst rangeLabel = rangeLabels[range] || 'Last 1 week';\nconst articles = $input.all().map(i => i.json).filter(a => a.id || a.title);\n\nfunction formatDate(date) {\n  return new Date(date).toLocaleDateString('en-US', {\n    weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'\n  });\n}\n\nfunction formatDateShort(date) {\n  return new Date(date).toLocaleDateString('en-US', {\n    month: 'short', day: 'numeric', year: 'numeric'\n  });\n}\n\nfunction topicDisplay(slug) {\n  if (!slug) return '';\n  return slug.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');\n}\n\nconst startDate = rangeStart;\n\nconst lines = [];\n\n// Header: same as Node weeklyDigest — \"📰 AI News Weekly Digest — {Topic} — Last 1 week\"\nconst titleParts = ['📰 AI News Weekly Digest', topicSlug ? topicDisplay(topicSlug) : null, rangeLabel].filter(Boolean);\nlines.push('# ' + titleParts.join(' — '));\nlines.push('');\nlines.push(`**${formatDate(startDate)} — ${formatDate(endDate)}**`);\nlines.push('');\n\nconst uniqueSources = new Set(articles.map(a => a.source));\nlines.push(`> ${articles.length} articles from ${uniqueSources.size} sources`);\nlines.push('');\nlines.push('---');\nlines.push('');\n\nif (articles.length === 0) {\n  lines.push('_No articles found for this period._');\n} else {\n  const grouped = {};\n  for (const article of articles) {\n    const cat = article.category || 'general';\n    if (!grouped[cat]) grouped[cat] = [];\n    grouped[cat].push(article);\n  }\n  const sortedCategories = Object.entries(grouped).sort(\n    (a, b) => b[1].length - a[1].length\n  );\n  for (const [category, catArticles] of sortedCategories) {\n    const displayCat = category.charAt(0).toUpperCase() + category.slice(1);\n    lines.push(`## ${displayCat} (${catArticles.length})`);\n    lines.push('');\n    catArticles.sort((a, b) => new Date(b.published_at) - new Date(a.published_at));\n    for (const article of catArticles) {\n      const date = article.published_at ? formatDateShort(article.published_at) : 'Date N/A';\n      const summary = article.summary\n        ? article.summary.slice(0, 200) + (article.summary.length > 200 ? '...' : '')\n        : '_No summary available._';\n      lines.push(`### [${article.title}](${article.url})`);\n      lines.push('');\n      lines.push(`📅 ${date} · 📡 ${article.source}`);\n      lines.push('');\n      lines.push(summary);\n      lines.push('');\n    }\n    lines.push('---');\n    lines.push('');\n  }\n}\n\nlines.push(`_Generated on ${formatDate(new Date())} by AI News Digest._`);\n\nconst markdown = lines.join('\\n');\n\nreturn [{\n  json: {\n    markdown,\n    articleCount: articles.length,\n    sourceCount: new Set(articles.map(a => a.source)).size,\n    categoryCount: new Set(articles.map(a => a.category || 'general')).size,\n    period: `${formatDateShort(startDate)} - ${formatDateShort(endDate)}`,\n    timestamp: new Date().toISOString()\n  }\n}];"
      },
      "id": "7a540ccb-5785-465e-b882-30658e24c555",
      "name": "Generate Markdown Digest",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2976,
        544
      ]
    },
    {
      "parameters": {
        "jsCode": "const data = $('Generate Markdown Digest').first().json;\n\nreturn [{\n  json: {\n    message: `Weekly digest generated. ${data.articleCount} articles across ${data.categoryCount} categories from ${data.sourceCount} sources.`,\n    articleCount: data.articleCount,\n    period: data.period,\n    markdown: data.markdown,\n    timestamp: data.timestamp\n  }\n}];"
      },
      "id": "08492762-5d45-4abf-bf21-a3859b1c8c3e",
      "name": "Digest Summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3392,
        544
      ],
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "operation": "toJson",
        "options": {
          "fileName": "data"
        }
      },
      "type": "n8n-nodes-base.convertToFile",
      "typeVersion": 1.1,
      "position": [
        3184,
        544
      ],
      "id": "e2e0a802-8c88-46b5-ab8b-838ac3c0a4f6",
      "name": "Convert to File"
    }
  ],
  "pinData": {},
  "connections": {
    "Digest Webhook": {
      "main": [
        [
          {
            "node": "Normalize Digest Params",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Normalize Digest Params",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Weekly (Mon 9AM)": {
      "main": [
        [
          {
            "node": "Normalize Digest Params",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Digest Params": {
      "main": [
        [
          {
            "node": "Fetch Last 7 Days Articles",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Last 7 Days Articles": {
      "main": [
        [
          {
            "node": "Generate Markdown Digest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Markdown Digest": {
      "main": [
        [
          {
            "node": "Convert to File",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Convert to File": {
      "main": [
        [
          {
            "node": "Digest Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "b29245a7-9ad8-4a9c-ac97-96271df8938d",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "6ccf58988494e01628f1f2b0b29fa215fe5dea960ad76ee7a83c15614a8f91e5"
  },
  "id": "GNmycKmlgND2TLmg",
  "tags": [
    {
      "updatedAt": "2026-03-10T17:25:58.271Z",
      "createdAt": "2026-03-10T17:25:58.271Z",
      "id": "ZkCAtpBXpHu84Zym",
      "name": "ai-news-digest"
    }
  ]
}